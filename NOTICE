# Notices

`shared/references/tlc-baseline.md` adapts methodology from `tlc-spec-driven`
(upstream `metadata.version: 3.3.0`, https://github.com/tech-leads-club/agent-skills —
`packages/skills-catalog/skills/(development)/tlc-spec-driven/SKILL.md`), originally authored
by Felipe Rodrigues / Tech Leads Club and made available under CC-BY-4.0. This package
rewrites and extends that baseline for a public, local-first workflow. This attribution does
not imply endorsement. See `LICENSING.md`.

`shared/references/tdd-baseline.md` is inspired by and adapted from the public `tdd` skill in
`mattpocock/skills` (https://github.com/mattpocock/skills —
`skills/engineering/tdd/SKILL.md`). That skill carries no version of its own; the pinned
reference point is the repository's release tag `v1.2.3`
(https://github.com/mattpocock/skills/releases/tag/v1.2.3). This attribution does not imply
endorsement. See `LICENSING.md`.

Both upstream version references are tracked in `shared/baselines/registry.yml`
(`upstream_version`) and are updated deliberately, never silently, when the upstream skill
changes — see docs/baselines.md.

The human-rich CLI bundle includes build-time copies of `@clack/prompts`, `@clack/core`,
`picocolors`, and `sisteransi`. Their licenses are reproduced in
`LICENSES/CLI-UI-BUNDLED.txt`.
