# Workflow

Create specs before implementation, implement one Task at a time, independently check
Task evidence, and run feature validation after integration.

Use multi-worktree orchestration only after explicit planning identifies independent
paths and dependencies. PR skills are extensions; they never replace task checking.
