# Context — quiet-hours-notifications

The feature serves a generic product team. Notification delivery is represented by the same
local domain record used by task-management; this feature only changes when that record's
delivery is triggered, not how it is stored.
