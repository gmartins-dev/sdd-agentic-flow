# task-management — T1

## Scope

Implements REQ-1 (task lifecycle fields) as the smallest tested increment: `createTask(input)`.

## Evidence

- `npm test -- task.spec.ts` — RED then GREEN, see `tasks.md#T1`.
- No unrelated files changed.
