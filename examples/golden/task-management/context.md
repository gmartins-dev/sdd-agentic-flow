# Context — task-management

The feature serves a generic product team. Existing task data can be extended; notification delivery is represented by a local domain event rather than a provider integration.
