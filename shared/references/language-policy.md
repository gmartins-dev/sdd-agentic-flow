# Language policy

Human-facing output follows `language.profile` and `language.human_outputs` in
`.sdd/config.yml`. Supported profiles are `en-US` and `pt-BR`.
Technical tokens, paths, commands, modes, status values, identifiers, and protocol
names remain canonical English and are not translated. Both profiles use
`language.bilingual_mode: technical-canonical`.
