# Language policy

Human-facing output follows `language.human_outputs` in `.sdd/config.yml`.
Technical tokens, paths, commands, modes, status values, identifiers, and protocol
names remain canonical English and are not translated.
